
<div class="row">
  <div class="col s12 fixed-categories">
    <% if current_user %>
      <% get_categories.limit(9).each do |cat| %>
        <a class="category-item" href=<%= "/section/#{cat.id}" %> >
          <%= cat.name %>
        </a>
      <% end %>
    <% end %>
  </div>
</div>
